# 📑 PROJECT: MAILDROP INFRASTRUCTURE DOCS
**Version:** 1.0.4 | **Domain:** yourdomain.com
**Note:** This document covers server-side setup. The Python Automation Script is attached separately.

---

## 1. 🛡️ VERCEL: BACKEND CONFIGURATION
لضمان وصول طلبات السكربت إلى قاعدة البيانات دون اعتراض أمني:

### Disable Deployment Protection:
1. اذهب إلى Settings > Deployment Protection.
2. اضبط Vercel Authentication على Disabled.

> **الأهمية:** هذا يسمح لسكربت البايثون والـ API بالاتصال بالسيرفر دون الحاجة لتسجيل دخول يدوي.

### Environment Variables:
يجب إضافة المفاتيح التالية:
- `ADMIN_PASSWORD`
- `REDIS_URL`

> [!WARNING]
> **تنبيه:** أي تغيير في هذه المفاتيح يتطلب عمل Redeploy للمشروع.

---

## 2. 🌐 CLOUDFLARE: SSL & NETWORKING
ضبط التوجيه والتشفير لضمان التوافق الكامل:

### SSL/TLS Settings:
- يجب اختيار الوضع **Flexible**.

> **الأهمية:** يمنع ظهور خطأ "Too many redirects" أو فشل الشهادات عند الربط بين Cloudflare و Vercel.

### DNS & Proxy:
- تأكد أن السحابة (Proxy) مفعلة (باللون البرتقالي) للدومين الخاص بك yourdomain.com.

---

## 3. ⚙️ CLOUDFLARE WORKER: THE ENGINE
هذا الجزء مسؤول عن استقبال الإيميلات وتحليلها وإرسالها إلى الـ API الخاص بك.

### طريقة إضافة الـ Worker:
1. **إنشاء الـ Worker:** اذهب إلى Workers & Pages في Cloudflare وأنشئ Worker جديداً.
2. **لصق كود الـ Worker:** يتم لصق الكود المرفق الذي يرسل الطلبات إلى `https://yourdomain.com/api/receive`.

### ربط الإيميل (Email Routing):
1. اذهب إلى تبويب Email للدومين الخاص بك.
2. اختر Email Routing ثم Routing Rules.
3. قم بتفعيل **Catch-all address**.
4. في خانة Action، اختر **Send to Worker** وحدد الـ Worker الذي أنشأته.

---

## 4. 🤖 AUTOMATION BOT (ATTACHED)
- **ملف السكربت:** `maildrop_bot.py` (مرفق مع هذا الدليل).
- **الوظيفة:** يقوم بتوليد الإيميل آلياً عبر الرابط `https://yourdomain.com/api/gen` ثم يراقب وصول الـ OTP.

### المميزات:
- واجهة سطر أوامر ملونة (أبيض وأخضر).
- مؤشر انتظار حركي (ساعة رملية ⏳).
- استخراج تلقائي للكود من الرسالة.

---

**Powered by TakiLabs**
